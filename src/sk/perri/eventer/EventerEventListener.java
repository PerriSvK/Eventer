package sk.perri.eventer;

import org.bukkit.event.Listener;

public class EventerEventListener implements Listener
{
    private Main plugin;

    EventerEventListener(Main plugin)
    {
        this.plugin = plugin;
    }
}
